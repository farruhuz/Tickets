import { Stops } from '../types/common';

const defaultFilters: Stops = {
  allStops: false,
  withoutStops: false,
  oneStop: false,
  twoStops: false,
  threeStops: false,
};

export default defaultFilters;
